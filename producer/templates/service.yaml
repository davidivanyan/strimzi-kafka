apiVersion: v1
kind: Service
metadata:
  name: {{ include "producer.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ include "producer.fullname" . }}
